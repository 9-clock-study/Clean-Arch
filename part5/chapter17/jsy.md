소프트웨어 아키텍처는 선을 긋는 기술이며, 이러한 선을 경계라고 부른다.
곙계는 sw 요소를 분리하고, 경계 한편에 있는 요소가 반대편에 있는 요소를 알지 못하도록 막는다.
핵심은 경계를 나누어 업무로직을 오염시키지 못하게 만들려는 목적이다.

> 아키텍트의 목표는 필요한 시스템을 만들고 유지하는 데 드는 인적 자원의 최소화

# 오해들...

- 업무 규칙이 구체화된 것이 바로 DB다 -> X
- GUI가 시스템이다 -> X

- 인터페이스 뒤에는 인터페이스를 조작하는 모델(데이터 구조와 함수로 구성된 정교한 집합)이 존재한다.!
  모델에게 인터페이스는 중요하지 않고 중요한 것은 업무 규칙!

> 근데 보통 최상위 인터페이스를 두고 소통할텐데 그럼 업무 규칙이 작성된게 인터페이스일텐데 이게 안중요?..(p180쪽 첫 번째 문단 중간)

# 플러그인 아키텍처

무튼 이렇게 나누다 보면 결국엔 플러그인 아키텍처를 패턴이 만들어짐,

# 플러그인에 대한 논의

우리가 시스템에서 갖추고자 하는 관계 : A -> B
A가 B에 의존할 때 A는 B를 건드릴 수 없음 오직 B가 A를 통제함(불합리한 구조)

왜냐 이렇게 해야 특정 모듈에 영향을 받지 않고, 업무규칙을 지킬 수 있음, 또한 경계를 나눌 수 있게 됨 <- 이 역시도 순전히 SRP에 해당함
